---
name: req-to-code
description: >-
  对话式渐进开发 skill。面向模糊或演进中的需求，适合迭代式结对编程。
  Round 1：写出最小可运行骨架，每个函数带完整的 8 字段头
  （ROLE/DEPENDS_ON/IN/OUT/OWNS_FIELDS/SIDE/ERRORS/LOG）但 body 是占位符
  （NotImplementedError 或 stub return，用 TODO 标记追踪）。
  Round 2+：根据用户反馈逐步填充函数 body，行为变化时更新头，
  按需新增函数。尽量少打断——能推断就推断，写完让用户看。
  只有在严重缺失信息会导致骨架结构性错误时才提问。
  每轮对变更函数跑 7 规则一致性检查；最终轮全量检查。
  自包含——无编排器，无子 skill。
  每次对话有主旨；每个方案回答三个问题：为什么这样、有什么缺点、有没有冲突。
---

# Req-To-Code — 对话式渐进开发

## 输出设计

通过多轮对话渐进式地构建代码。能推断的推断，尽量少打断。每轮输出一份设计文档，用户只看设计文档——通过设计文档理解方案、审查决策、给出反馈。

设计文档包含：主旨、方案三问、完整可运行代码、进度摘要。

核心规则：**每轮都输出完整可运行的代码。** Round 1 是骨架（完整头 + 占位符 body）。后续轮次填充 body。用户始终能看到能跑的东西。

## 何时使用

- 用户描述业务需求，期望迭代式地构建代码
- 需求模糊或演进中——用户想通过对话逐步明确
- 用户想要结对编程风格："先搭个骨架" / "一步步来" / "先写个第一版"
- 用户说"先搭结构，再慢慢填"

## 何时不用

- 给已有代码补结构化头 → 用 `$code-formatter`
- 对已有代码做事后数据流 / 影响分析 → 用 `$flow-tracer`
- 用户想要完整的"分析→设计→编码"流水线且每阶段确认

## 函数头标准

每个函数必须带结构化头/文档字符串。使用语言原生风格：

### Python

```python
def function_name(...):
    """
    ROLE:
      <一句话业务职责>

    DEPENDS_ON:
      [upstream_function_a, upstream_function_b]

    IN:
      <输入类型和业务含义>

    OUT:
      <输出类型和业务含义>

    OWNS_FIELDS:
      [field_a, field_b]

    SIDE:
      <INSERT/UPDATE/DELETE/外部调用/文件写入等；无副作用写 None>

    ERRORS:
      <异常、降级或 None 返回语义；无则 None>

    LOG:
      IN/OUT 使用 INFO；关键业务 MAP 使用 INFO；批量细节 DEBUG；可恢复异常 WARNING；中断性异常 ERROR。
    """
```

### JavaScript / TypeScript / Java / C / C++

使用相同的 8 字段结构，采用语言原生格式（JSDoc `/** */`、Javadoc `/** */` 或块注释 `/* */`），按语言习惯放置。

---

## 思考纪律

每次对话都有一个主旨——这轮工作要达成的目标。所有决策都要对齐它。

### 主旨驱动

主旨不是一开始就明确的，而是通过多轮对话慢慢总结出来的。早期轮次主旨可能模糊，随着对话推进逐渐清晰。

当主旨逐渐清晰时，用一句话记录下来：

> 这次的目标是：<一句话说清要解决什么问题>

主旨明确后，后续的函数分解、填充顺序选择、方向变更都要对齐它。如果用户反馈似乎偏离了主旨，先检查：
- 主旨变了？→ 显式更新主旨。
- 还是只是绕一下？→ 记下来，保持主旨不变。

### 每个方案回答三个问题

当你提出任何东西——骨架分解、填充方式、方向变更——都要在方案旁边回答三个问题：

| 问题 | 要想什么 |
|---|---|
| **为什么这样？** | 动机是什么？为什么选这个方案而不是另一个？ |
| **有什么缺点？** | 这个方案假设了什么？哪里会失效？ |
| **有没有冲突？** | 和已有的函数、头、之前的决策有没有矛盾？ |

用户应该不只看到你提了*什么*，还要看到*为什么*以及*哪里弱*。

### 示例

```
骨架方案：read_file → clean_data → validate_rows → transform → write_output → main

为什么这样分：
  数据流驱动，每步处理一个阶段，read→clean→validate→transform→write 是经典 ETL 骨架。

有什么缺点：
  - 假设单一文件输入；如果需要多文件合并，read_file 要拆成 read+merge 两步
  - validate 和 clean 的顺序可调——先校验再清洗可能更合理（取决于数据脏的程度）

有没有冲突：
  无。各函数 OWNS_FIELDS 不重叠，DEPENDS_ON 形成线性 DAG，无循环。
```

---

## 工作流程

### Round 1：搭骨架

#### 步骤 1：快速理解 + 定义主旨

评估用户请求的以下维度：

| 维度 | 要找什么 |
|---|---|
| 目的 | 解决什么业务问题？ |
| 输入 | 输入数据是什么形状和含义？ |
| 输出 | 输出用来干什么？ |
| 流程 | 高层数据流向是什么？ |

**提问边界**——只在严重缺失信息会导致骨架结构性错误时才问：

| 情况 | 动作 |
|---|---|
| 输入格式未知（文件？API？数据库？内存？） | **问**——猜错会导致整个骨架废掉 |
| 核心目的有两种同样合理的解释 | **问**——方向错会浪费一整轮 |
| 目标输出完全未知 | **问**——没有它定义不了 OUT |
| 函数分解策略 | **推断**——选一个合理的分解，用户可以调整 |
| 错误处理方式 | **推断**——用合理的默认值，写在 ERRORS 里 |
| 日志级别和格式 | **推断**——遵循标准 |
| 变量命名 / 类型标注 | **推断**——用户可以之后改 |
| 用 async 还是 sync | **推断**——选更简单的默认值，标注一下 |

提问时，把所有问题合并成一轮问完。把接近确定的假设表达为假设：

> 我假设输入是 CSV 文件、输出也是 CSV。如果不对请告诉我，否则我先按这个搭骨架。

不要在写骨架之前请求确认骨架设计。写出来，让用户看。

**然后尝试用一句话总结主旨（可能还不清晰，没关系）：**

> 这次的目标是：<一句话说清要解决什么问题>

主旨可能在早期轮次还比较模糊，随着对话推进会逐渐清晰。当前能总结多少就写多少，后续轮次再修正。

#### 步骤 2：识别核心函数

根据数据流，每个独立的业务操作对应一个函数：

- 输入 / 解析
- 校验
- 核心转换（可能多个）
- 输出 / 持久化
- 入口 / 编排

保持最小化——不要过度分解。骨架展示的是核心结构，不是每个辅助函数。目标 3-7 个核心函数。

#### 步骤 3：写骨架

对每个核心函数：

1. **写完整的 8 字段头**——这是设计契约。虽然 body 是占位符，但头必须完整，准确描述*预期*行为。

2. **写占位符 body**，用以下两种模式之一：

**模式 A — Raise（关键路径，调用占位符应该响亮地失败）：**

```python
def clean_data(raw_df: pd.DataFrame) -> pd.DataFrame:
    """
    ROLE:
      对原始数据去重去空并标准化字段名

    DEPENDS_ON:
      [read_file]

    IN:
      raw_df: pd.DataFrame — 原始未清洗数据

    OUT:
      pd.DataFrame — 清洗后的数据

    OWNS_FIELDS:
      [cleaned_df]

    SIDE:
      None

    ERRORS:
      None

    LOG:
      IN INFO (row count); OUT INFO (cleaned row count, removed count)
    """
    # TODO(round-1): 实现去重去空逻辑
    raise NotImplementedError("去重去空逻辑待实现")
```

**模式 B — Stub 返回（骨架需要端到端跑通以验证结构）：**

```python
    # TODO(round-1): 实现去重去空逻辑
    return raw_df.copy()  # stub: 暂不处理，原样返回
```

根据用户是否需要端到端运行骨架来选择：

| 模式 | 何时使用 |
|---|---|
| `raise NotImplementedError` | 关键路径；stub 会掩盖结构问题 |
| `return <stub>` | 用户想跑通骨架验证管道形状 |

3. **包含入口函数**——一个 `main()` 或等价函数，按数据流顺序调用各函数。

#### 步骤 4：输出骨架 + 进度摘要 + 三个问题

输出三样东西：

1. **完整的骨架代码**——所有函数含头 + 占位符。必须语法有效、可导入。

2. **骨架方案的三个问题回答：**

```
骨架方案：<函数列表和数据流>

为什么这样分：
  <动机：为什么这样分解>

有什么缺点：
  <局限：这个方案假设了什么，哪里会失效>

有没有冲突：
  <一致性：和已有决策有没有矛盾>
```

3. **进度摘要：**

```
ROUND 1 — 骨架
══════════════════
主旨：<一句话主旨>

函数数：6
  ✅ 头完整：    6
  🔲 body 填充： 0
  📝 TODO：     6

TODO 清单：
  1. read_file       — 读取 CSV 文件为 DataFrame
  2. clean_data      — 去重去空并标准化字段名
  3. validate_rows   — 校验必填字段
  4. transform       — 字段映射和格式转换
  5. write_output    — 写入结果文件
  6. main            — 串联管道入口

下一步：告诉我先实现哪个，或直接说"继续"我按数据流顺序填充。
```

Round 1 不要跑 7 规则一致性检查——占位符 body 没有真实逻辑可查。

---

### Round 2+：增量填充

#### 步骤 1：读取当前状态

扫描已有代码：函数头、`# TODO(round-N)` 标记、`DEPENDS_ON` DAG。

#### 步骤 2：决定填充什么

根据用户输入：

| 用户说 | 动作 |
|---|---|
| "实现 clean_data" / 指定函数名 | 填充该函数 |
| "继续" / "next" / "继续填" | 按数据流顺序填充（upstream first） |
| 新需求 / 方向变更 | 见下方"处理方向变更" |
| 对已填充代码的反馈 | 修改指定函数 |

**默认填充顺序**：upstream first——先填 `DEPENDS_ON` 已经填好的函数。

如果存在多种实现方式，在编码前简要说明选了哪种、为什么、缺点是什么。只有一种合理方式 → 直接写。

#### 步骤 3：填充函数 body

对本轮要填充的每个函数：

1. **移除占位符**——删除 `# TODO(round-N)` 标记和 `raise` / stub。

2. **实现 body**——目标 ≤30 行。使用管道式变量名揭示转换链：

```python
raw_headers -> cleaned_headers -> header_map
```

不要对不同的语义状态复用同一个变量名。

3. **添加结构化日志**，匹配 `LOG` 头字段：

| 阶段 | 级别 | 何时使用 |
|---|---|---|
| IN | INFO | 关键输入用于追踪 |
| OUT | INFO | 关键输出或结果摘要 |
| MAP 单条 | INFO | 业务关键字段映射 |
| MAP 批量 | DEBUG | 批量映射、逐行细节 |
| ERR 可恢复 | WARNING | 降级、跳过、重试 |
| ERR 致命 | ERROR | 操作中断 |

日志格式：`业务动作名 | 阶段(IN/MAP/ERR/OUT) | key=value`

不要记录密钥、密码、token、完整 PII 或大体积数据。

4. **行为变化时更新头：**

| 填充时发现... | 更新的头字段 |
|---|---|
| 新建/修改了字段 | `OWNS_FIELDS` |
| 新的副作用 | `SIDE` |
| 新的异常或降级 | `ERRORS` |
| `LOG` 里没有的新日志点 | `LOG` |
| 输入用法变了 | `IN` |
| 返回形状变了 | `OUT` |
| 调用了新的上游依赖 | `DEPENDS_ON` |

#### 步骤 4：跑一致性检查（仅变更函数）

对本轮变更的每个函数跑 7 规则检查：

| # | 规则 | STALE（头多声明了） | MISSING（头少声明了） |
|---|---|---|---|
| 1 | `OWNS_FIELDS_MATCH` | 声明的字段 body 没操作 | body 操作了未声明的字段 |
| 2 | `DEPENDS_ON_MATCH` | 声明的依赖没调用 | body 调用了未声明的函数 |
| 3 | `SIDE_MATCH` | 声明的副作用没执行 | body 有写操作但 `SIDE: None` |
| 4 | `IN_MATCH` | 描述的参数不在签名里 | 签名参数没描述 |
| 5 | `OUT_MATCH` | 声明的返回形状从没返回 | 返回了未声明的形状 |
| 6 | `LOG_MATCH` | 描述的日志没有对应语句 | 有日志但 `LOG` 没描述 |
| 7 | `ERRORS_MATCH` | 描述的异常没有对应代码 | body 抛出/处理了未声明的异常 |

输出前修复所有 STALE 和 MISSING。

#### 步骤 5：输出更新后的代码 + 进度

输出：

1. **完整的更新后代码**——每轮都是完整可运行状态。

2. **进度摘要：**

```
ROUND 2 — 填充
══════════════
主旨：<一句话主旨>
主旨对齐：✓ 本轮填充仍对齐主旨

本轮填充：read_file, clean_data

进度：
  ✅ 头完整：    6
  ✅ body 填充： 2  （本轮 +2）
  📝 TODO：     4

一致性检查（变更函数）：
汇总：2 个检查，0 STALE，0 MISSING

TODO 清单：
  1. read_file       ✅
  2. clean_data      ✅
  3. validate_rows
  4. transform
  5. write_output
  6. main

下一步：继续填充 validate_rows，或告诉我调整方向。
```

---

### 处理方向变更

用户中途改需求（新功能、不同方案、范围变化）时：

1. **重新读取当前状态**——什么已建，什么还是占位符。
2. **评估影响**——哪些函数受影响。
3. **回答三个问题**：
   - 为什么改：变更的动机
   - 有什么缺点：变更破坏了什么或假设了什么
   - 有没有冲突：和已填充函数的矛盾

4. **直接应用变更**（除非有歧义才问）：

   | 影响 | 动作 |
   |---|---|
   | 已填充函数行为要变 | 更新头，body 替换为 `# TODO(round-N): rework — <原因>` + 占位符 |
   | 函数不再需要 | 删除；更新引用它的函数的 `DEPENDS_ON` |
   | 需要新函数 | 添加完整头 + 占位符 body；更新下游 `DEPENDS_ON` |
   | 输入/输出格式变了 | 传播到受影响函数的 `IN`/`OUT`/`OWNS_FIELDS` |

5. **更新 DEPENDS_ON DAG。**

6. **输出改版后的代码 + 变更摘要（含三个问题回答）：**

```
方向变更（round 3）
══════════════════════════
主旨：<主旨，如果变了标注 NEW>

用户反馈：改成支持 JSON 输入而不是 CSV

为什么改：<动机>
有什么缺点：<局限>
有没有冲突：<和已有函数的矛盾>

影响：
  - read_file       → rework：改为读取 JSON
  - clean_data      → 不受影响
  - transform       → rework：JSON 结构映射不同
  - write_output    → rework：改为写入 JSON
  - （新增）load_json_config → 新函数

rework 的函数标记 # TODO(round-3): rework
DEPENDS_ON 已更新。
```

只在方向变更有歧义时才问：
> 你是只想支持 JSON，还是 CSV 和 JSON 都要支持？

---

### 最终轮：完成

所有 TODO 填完后：

1. **对所有函数跑全量 7 规则一致性检查。**
2. **验证字段归属**——每个可变/派生字段有且只有一个归属函数。
3. **验证 DEPENDS_ON DAG**——所有依赖存在且正确。
4. **验证日志**——无密钥泄露，级别合理，`LOG` 与语句匹配。
5. **主旨对齐检查**——所有函数是否都服务于主旨？如果主旨中途变过，确认代码对齐的是最新版主旨。

6. **输出最终代码 + 完成摘要：**

```
ROUND 4 — 完成
══════════════════
主旨：<最终主旨>
主旨对齐：✓ 所有函数服务于主旨

所有函数已填充。最终一致性检查通过。

函数数：6
  ✅ 头完整：    6
  ✅ body 填充： 6
  📝 TODO：     0

一致性检查（全部函数）：
汇总：6 个检查，0 STALE，0 MISSING

字段归属：所有派生字段有且只有一个归属 ✓
DEPENDS_ON DAG：所有依赖已验证 ✓
日志检查：无密钥泄露，级别合理 ✓
主旨对齐：已确认 ✓

代码就绪。
```

---

## 规则

- **每轮输出完整可运行代码**——不要片段或局部文件。
- **每次对话有主旨**——Round 1 陈述，每轮检查对齐。
- **每个方案回答三个问题**——为什么这样、有什么缺点、有没有冲突。
- **函数头是持久真实来源**——Round 1 就完整，行为变化时更新。
- **占位符 body** 使用 `# TODO(round-N): <描述>` + `raise NotImplementedError` 或 stub 返回。
- **尽量少提问**——能推断就推断，写完让用户看。
- **只在严重缺失信息会导致骨架结构性错误时才问。**
- **用户说"继续"时 upstream first** 填充。
- **填充改变行为时更新头。**
- **每轮对变更函数跑 7 规则一致性检查**；最终轮全量检查。
- **方向变更**：直接应用，标记 rework 函数，更新 DAG，回答三个问题。有歧义才问。
- **头里全用业务语言**——不要实现术语。
- **日志不暴露密钥**——记数量、ID、摘要。
- **每个可变/派生字段有且只有一个归属函数。**
